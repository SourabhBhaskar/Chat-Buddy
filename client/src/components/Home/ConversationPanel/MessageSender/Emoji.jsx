import React from 'react';
import { icons } from '../../../../utils/icons.util';
import IconButton from '../../IconButton';


function Emoji() {
  return (
    <IconButton icon={icons.emoji} />
  )
}

export default Emoji;