// Find the index of the element to move
// const indexToMove = array.indexOf(elementToMove);

// // Remove the element from its current position
// if (indexToMove > -1) {
//   array.splice(indexToMove, 1);
// }