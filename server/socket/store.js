// Declarations
const userId_socketId = new Map();
const socketId_userId = new Map();
const userId_messages = new Map();


module.exports = {
    userId_socketId,
    socketId_userId,
    userId_messages
}