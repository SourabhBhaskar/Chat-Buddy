// Declarations
const userId_socketId = new Map();
const userId_messages = new Map();
const socketId_user = new Map();


module.exports = {
    userId_socketId,
    userId_messages,
    socketId_user,
}